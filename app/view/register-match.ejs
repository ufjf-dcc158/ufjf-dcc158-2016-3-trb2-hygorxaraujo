<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <title>Trabalho 2 - Laboratório de Programação VI</title>
</head>
<body>
<h1>Agendar Partida</h1>
<form method="POST" action="/matches" id="center">
    <label>
        Data:
        <input type="datetime-local" name="date" required/>
    </label><br/><br/>
    <label>
        Jogador 1:
        <select name="playerOneId">
            <%
            for(var i = 0;i < players.length;i++){
                var playerOne = players[i];
            %>
            <option value="<%= playerOne._id %>">
                <%= playerOne.firstname + " " + playerOne.lastname %>
            </option>
            <%
            }
            %>
        </select>
    </label><br/><br/>
    <label>
        Jogador 2:
        <select name="playerTwoId">
            <%
            for(var j = 0;j < players.length;j++){
                var playerTwo = players[j];
            %>
            <option value="<%= playerTwo._id %>">
                <%= playerTwo.firstname + " " + playerTwo.lastname %>
            </option>
            <%
            }
            %>
        </select>
    </label><br/><br/>
    <input type="submit" value="Agendar">
</form>
</body>
</html>
